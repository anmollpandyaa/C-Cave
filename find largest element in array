#include<stdio.h>

int main()
{
    int arr[] = {2, 8, 9, 16, 4};

    int largest = arr[0];

    for (int i = 0; i < 5; i++)
    {
        if (arr[i] > largest)
        {
            largest = arr[i];
        }
        
    }

    printf("%d is the largest element", largest);
    return 0;
}
