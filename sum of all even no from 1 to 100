#include <stdio.h>

int main()
{
    int sum = 0;

    for (int i = 0; i <= 100; i++)
    {
        if (i % 2 == 0)
        {
            sum += i;
        }
    }

    printf("Sum of all even numbers from 1 to 100 is %d", sum);

    return 0;
}
